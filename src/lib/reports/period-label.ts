/**
 * Demo transaction history window — for UI labels on unfiltered totals only.
 * Keep in sync with seeded statement range (Sep 2025–Aug 2026).
 */
export const ALL_PERIODS_HINT = "All periods (Sep 2025–Aug 2026)";
